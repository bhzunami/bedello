<% provide(:title, 'Kategorien') %>
<h3 class="h3 mb-3 font-weight-normal">Kategorien</h3>
<table class="table table-hover">
  <thead class="thead-light">
  <tr>
    <th scope="col">#</th>
    <th scope="col">Name</th>
    <th scope="col">Beschreibung</th>
    <th scope="col">Angelegt am</th>
		<th scope="col"></th>
    </tr>
  </thead>
  <tbody>
	<% @categories.each do |c| %>
	<tr>
	<th scope="row" class="col-md-1"><%= c.category_order %></th>
		<td class="col-md-2"><%= c.name %> </td>
		<td class="col-md-5"><%= c.description %></td>
		<td class="col-md-2"><%= l c.created_at, format: :swiss %></td>
		<td class="text-right"><%= link_to "Edit", edit_category_path(c), class: "btn btn-primary btn-sm" %>
			<%= link_to "Löschen", c, data: {confirm: 'Bist du sicher?'}, method: :delete, class: "btn btn-sm btn-danger" %>
		</td>
	</tr>
	<% end %>
	</tbody>
</table>
		<%= link_to "Neue Kategorie", new_category_path, class: "btn btn-primary btn-success" %>
	</div>
</div>
	