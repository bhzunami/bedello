<ul class="nav nav-list">
  <li class="nav-header">Categories</li>

  <% categories.each do |c| %>
  <li>
    <%= link_to c.name, products_path(category_id: c.id), id: c.id %>
  </li>

  <% end %>
</ul>

  