<%= simple_form_for(@user, html: {class: 'form-horizontal' }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :password_confirmation, label: "Bestätigung" %>

   <%= f.submit "Speichern", class: "btn btn-large btn-primary" %>
<% end %>
